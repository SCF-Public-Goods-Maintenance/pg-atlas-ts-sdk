name: Manual Release

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Do dry-run? (true/false)"
        required: true
        default: "true"

jobs:
  release:
    name: 🚀 Release package
    runs-on: ubuntu-latest

    steps:
      - uses: actions/create-github-app-token@v3.1.1
        id: app-token
        with:
          client-id: ${{ vars.GH_RELEASE_APP_CLIENT_ID }}
          private-key: ${{ secrets.GH_RELEASE_APP_PRIVATE_KEY }}

      - name: Get Release App User ID
        id: get-user-id
        run: echo "user-id=$(gh api "/users/${{ steps.app-token.outputs.app-slug }}[bot]" --jq .id)" >> "$GITHUB_OUTPUT"
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}

      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          token: ${{ steps.app-token.outputs.token }}
          ref: ${{ github.ref }}
          persist-credentials: false

      # Security note: checkout uses the app token only to clone the repository,
      # but does not persist credentials for later steps. This prevents unrelated
      # workflow code from accidentally reusing release bot permissions.
      # The release bot token is applied explicitly only to the push commands below.

      - name: Setup pnpm
        uses: pnpm/action-setup@v5
        with:
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "24"
          registry-url: "https://registry.npmjs.org/"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Update to latest openapi.json
        run: pnpm run update

      - name: Rewrite tests for regenerated SDK
        run: pnpm run rewrite-tests

      - name: Run tests
        run: pnpm run test

      - name: Build
        run: pnpm run build

      - name: Get version from package.json
        id: get_version
        run: |
          VERSION=$(node -p "require('./package.json').version")
          echo "VER=$VERSION" >> $GITHUB_OUTPUT

      # Accepted risk: we update the local checkout's origin URL to embed the
      # release app token. This is a repo-local change in the ephemeral runner
      # workspace only, not a global git config change. The origin URL carries
      # the token until the job ends, but no other workflow step should reuse it
      # except the explicit release pushes below.
      - name: Commit release bump
        run: |
          V=${{ steps.get_version.outputs.VER }}
          git add .
          git remote set-url origin https://x-access-token:${{ steps.app-token.outputs.token }}@github.com/${{ github.repository }}.git
          if ! git diff --cached --quiet; then
            git -c user.name='${{ steps.app-token.outputs.app-slug }}[bot]' -c user.email='${{ steps.get-user-id.outputs.user-id }}+${{ steps.app-token.outputs.app-slug }}[bot]@users.noreply.github.com' commit -m "prepare release $V"
            git push origin HEAD:${{ github.ref_name }}
          else
            echo "⏭ nothing to commit, continuing…"
          fi

      - name: Create & push tag
        if: ${{ github.event.inputs.dry_run == 'false' }}
        run: |
          V=${{ steps.get_version.outputs.VER }}
          git tag -a "v$V" -m "Release v$V"
          git push origin "v$V"

      - name: Publish to npm
        run: |
          if [ "${{ github.event.inputs.dry_run }}" = "false" ]; then
            echo "=== REAL PUBLISH ==="
            # --provenance links this build to this GitHub Action run
            pnpm publish --access public --provenance --no-git-checks
          else
            echo "=== DRY RUN ==="
            pnpm publish --dry-run --access public --no-git-checks
          fi
